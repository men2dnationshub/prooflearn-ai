# ProofLearn AI v0.12 does not require secrets.
# Add future API credentials here for local development only, then configure
# the same TOML values in the deployment platform's secret settings.
# Never commit the real .streamlit/secrets.toml file.
